import {StyleSheet} from 'react-native';

export const TRASH_WIDTH = 14;
export const TRASH_HEIGHT = 16;

export const HEART_WIDTH = 16;
export const HEART_HEIGHT = 14;

export const styles = StyleSheet.create({
  swipeButton: {
    backgroundColor: '#DF2C2C',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 20,
    marginHorizontal: 8,
  },
});
