import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  counterButton: {
    padding: 5,
  },
  counterText: {
    color: '#F4F4F7',
    fontSize: 15,
    lineHeight: 18,
    textAlign: 'center',
    paddingHorizontal: 2,
  },
});
